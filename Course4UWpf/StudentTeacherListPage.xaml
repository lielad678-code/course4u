<Page x:Class="Course4UWpf.StudentTeacherListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Course4UWpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StudentTeacherListPage">

    <Grid>
        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>


        <DataGrid x:Name="dgStudentTeacherList" Grid.Row="1" Grid.Column="1" AutoGenerateColumns="True" SelectionChanged="dgStudentTeacherList_SelectionChanged">
           

            <!-- Columns can be explicitly defined here if AutoGenerateColumns is false -->
        </DataGrid>
    </Grid>
</Page>
