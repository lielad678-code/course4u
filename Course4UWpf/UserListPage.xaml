<Page x:Class="Course4UWpf.TeacherListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Course4UWpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TeacherListPage">

    <Grid Margin="10">
        <DataGrid x:Name="TeacherGrid" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  HorizontalGridLinesBrush="LightGray"
                  VerticalGridLinesBrush="LightGray">

            <DataGrid.Columns>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*" />
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*" />
                <DataGridTextColumn Header="Password" Binding="{Binding Password}" Width="Auto" />
                <DataGridTextColumn Header="Birth Date" Binding="{Binding BirthDate, StringFormat=yyyy-MM-dd}" Width="Auto" />
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="Auto" />
                <DataGridTextColumn Header="Phone Number" Binding="{Binding PhoneNumber}" Width="Auto" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Page>