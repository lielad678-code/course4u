<Window x:Class="Course4UWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800"
        Background="#FFFFFDE7"
        WindowState="Maximized"
        FontFamily="Segoe UI">

    <Window.Resources>

        <SolidColorBrush x:Key="PrimaryBrush" Color="#2D2F33"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#DCE6F1"/>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10 8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="{StaticResource PrimaryBrush}">

            <TextBlock Text="☰  Menu"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="12"/>

            <Button x:Name="btnScehedule"
                    Content="📅  Schedule"
                    Click="Schedule_Click"
                    Style="{StaticResource MenuButtonStyle}"/>

            <Button x:Name="btnCourses"
                    Content="📚  Courses"
                    Click="Courses_Click"
                    Style="{StaticResource MenuButtonStyle}"/>

            <Button x:Name="btnTeacherList"
                    Content="👨‍🏫  Teachers List"
                    Click="TeacherList_Click"
                    Style="{StaticResource MenuButtonStyle}"/>

            <Button x:Name="btnStudentList"
                    Content="🎓  Students List"
                    Click="StudentList_Click"
                    Style="{StaticResource MenuButtonStyle}"/>

            <Button x:Name="btnAttendance"
                    Content="📝  Attendance"
                    Click="Attendance_Click"
                    Style="{StaticResource MenuButtonStyle}"/>

            <Button x:Name="btnSignNewTeacher"
                    Content="➕  Sign New Teacher"
                    Click="SignNewTeacher_Click"
                    Style="{StaticResource MenuButtonStyle}"/>

            <Button Content="⚙  Settings"
                    Click="Settings_Click"
                    Style="{StaticResource MenuButtonStyle}"/>

            <Button Content="🚪  Log Out"
                    Click="LogOut_Click"
                    Style="{StaticResource MenuButtonStyle}"/>

        </StackPanel>

        <Grid Grid.Column="1" Margin="12">
            <Border Background="White"
                    CornerRadius="8"
                    Margin="8">

                <Frame x:Name="MainFrame"
                        NavigationUIVisibility="Hidden"/>

            </Border>
        </Grid>

    </Grid>
</Window>